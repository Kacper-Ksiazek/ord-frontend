# E2E test environment — copy to .env.e2e at the repo root.
# Loaded automatically by e2e/fixtures/test-env.ts on import (via dotenv).
#
# Standard dotenv syntax: KEY=value, optional quotes, export prefix, # comments.

# Frontend URL (Playwright baseURL)
E2E_BASE_URL=http://localhost:5173

# Backend API URL (passed to dev server as PUBLIC_API_URL)
E2E_API_URL=http://localhost:8080

# Test user email — worker 0 default; parallel runs map workerIndex → e2e-ci-w{n}@ord.test
E2E_TEST_EMAIL=e2e-ci-w0@ord.test

# Parallel Playwright workers (ord-api Flyway V22 seeds e2e-ci-w0 … w3)
E2E_WORKER_COUNT=4
E2E_EMAIL_PREFIX=e2e-ci-w
E2E_EMAIL_DOMAIN=ord.test

# Fixed OTP code — must match OTP_CODE_FOR_WHITELISTED_EMAILS on the backend
E2E_OTP_CODE=123456

# Option B: HTTP endpoint returning latest OTP as JSON { "code": "123456" }
# E2E_OTP_FETCH_URL=http://localhost:8080/api/v1/test/otp

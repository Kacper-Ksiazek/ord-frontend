import type { FeedbackTabFilters } from '../feedback-tab.types';

export const DEFAULT_FEEDBACK_FILTERS: FeedbackTabFilters = {
	tab: 'ALL',
	severity: 'ALL',
	suggestionType: 'ALL',
	searchQuery: ''
};
